# bespoke-camera-ai — training + export + edge inference
# Install: pip install -r requirements.txt

# Core ML (training + export)
torch>=2.1
torchvision>=0.16

# Video / image I/O
opencv-python>=4.8

# ONNX export + quantization
onnx>=1.15
onnxruntime>=1.17          # CPU inference on edge
# onnxruntime-gpu>=1.17    # uncomment for GPU inference on server

# Numerical
numpy>=1.24

# ── Edge-only install (no PyTorch needed for predict.py) ──────────────────────
# On edge device, install only:
#   pip install onnxruntime opencv-python numpy
